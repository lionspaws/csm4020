function setup() {
  createCanvas(400, 400);
  background(220);
  
  square(130, 220, 150);       // x, y, size side
  circle(70, 125, 100);        // x, y, diameter (width)
  ellipse(300, 100, 150, 60);  // x, y, width, height
}